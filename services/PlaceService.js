const placesRepository = require("../repository/Places");

class PlacesService {
  constructor() {}

  async findPlace(place_id) {
    //    const placeId  =
  }
}

module.exports = new PlacesService(placesRepository);
